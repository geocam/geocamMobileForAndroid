<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="gov.nasa.arc.geocam"
      android:versionCode="1"
      android:versionName="1.0.0">
	
	<uses-permission android:name="android.permission.INTERNET" />  
	<uses-permission android:name="android.permission.CAMERA" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
	
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".GeoCamMobile"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

		<!-- There is a bug in the camera preview API, it only works in landscape orientation -->
		<activity android:label="@string/camera_name" android:name=".CameraActivity" 
				  android:screenOrientation="landscape">		
		</activity>

		<activity android:label="@string/camera_preview_name" android:name=".CameraPreviewActivity" 
				  android:screenOrientation="landscape">		
		</activity>

        <activity android:label="@string/upload_photos_name" android:name="UploadPhotosActivity">
	    </activity>

    </application>
</manifest> 